// SurveyResults.jsx  (protected — requires login)
// Screen 3 + Screen 4: Shows charts for each question and the AI insight button
// Fetches survey, questions, and responses from Supabase using the survey ID from the URL
// Renders a QuestionChart component for each question
// Has a "Generate Insight" button that calls POST /api/insight on the backend
// Ask Claude: "Write me the SurveyResults page in React that shows Recharts bar charts and calls the backend for AI insight"
