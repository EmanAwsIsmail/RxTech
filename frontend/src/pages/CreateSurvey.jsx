// CreateSurvey.jsx  (protected — requires login)
// Screen 1: Entrepreneur creates a survey
// Fields: title, description, up to 5 questions
// Each question has: question text, type (yes_no / multiple_choice / short_text), and options if multiple choice
// On submit: saves survey and questions to Supabase, then redirects to /dashboard
// Ask Claude: "Write me the CreateSurvey page in React that saves a survey with questions to Supabase"
